<!DOCTYPE html>
<html class="h-full bg-gray-100">
  <head>
    <title><%= content_for?(:title) ? ("#{yield(:title)} - Vibe Wars").html_safe : "Vibe Wars: NFT Visual Battles" %></title>
    <meta name="title" content="Vibe Wars">
    <meta name="description" content="<%= content_for?(:description) ? (yield(:description)) : "Find out which NFTs are the most visually desirable! In a set of thousands of NFTs, which ones do people actually like the look of the most?" %>">

    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <script src="https://kit.fontawesome.com/58f3deb2bd.js" crossorigin="anonymous"></script>
    <script src="//unpkg.com/alpinejs" defer></script>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Space+Grotesk:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <%#= stylesheet_link_tag "inter-font", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <script src="https://cdn.usefathom.com/script.js" data-site="MLLOEFAH" defer></script>
  </head>

  <body class="h-full">
      <div class="min-h-full">
        <nav class="bg-indigo-600">
          <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
            <div class="flex items-center justify-between h-16">
              <div class="flex items-center">
                <div class="flex-shrink-0">
                  <p class="text-4xl text-center text-white align-middle">
                  <a href="/">
                    <i class="fa-duotone fa-poo-storm"></i>
                    <span class="text-3xl font-bold align-top logo">Vibe Wars</span>
                  </a>
                  </p>
                </div>
                <div class="hidden md:block">
                  <div class="ml-10 flex items-baseline space-x-4">
                    <!-- Current: "bg-indigo-700 text-white", Default: "text-white hover:bg-indigo-500 hover:bg-opacity-75" -->
                    <%# <a href="#" class="bg-indigo-700 text-white px-3 py-2 rounded-md text-sm font-medium" aria-current="page">Dashboard</a> %>
                  </div>
                </div>
              </div>

              <div class="relative inline-block text-left" x-data="{open: false}" @mouseover="open = true" @mouseleave="open = false">
                <div class="relative flex items-center space-x-1 cursor-pointer text-sm font-medium">
                  
                    <button type="button" class="inline-flex justify-center w-full rounded-md border border-gray-300 shadow-sm px-4 py-2 bg-white text-sm font-medium text-gray-700 hover:bg-gray-50 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-offset-gray-100 focus:ring-indigo-500" id="menu-button" aria-expanded="true" aria-haspopup="true">
                      Collections
                      <!-- Heroicon name: solid/chevron-down -->
                      <svg class="-mr-1 ml-2 h-5 w-5" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 20 20" fill="currentColor" aria-hidden="true">
                        <path fill-rule="evenodd" d="M5.293 7.293a1 1 0 011.414 0L10 10.586l3.293-3.293a1 1 0 111.414 1.414l-4 4a1 1 0 01-1.414 0l-4-4a1 1 0 010-1.414z" clip-rule="evenodd" />
                      </svg>
                    </button>
                  

                <div class="origin-top-right top-7 absolute right-0 mt-2 w-72 rounded-md shadow-lg bg-white ring-1 ring-black ring-opacity-5 divide-y divide-gray-100 focus:outline-none" x-show="open" x-cloak role="menu" aria-orientation="vertical" aria-labelledby="menu-button" tabindex="-1" style="z-index:1000">
                
                  <div class="py-1" role="none">
                    <% @all_collections.each do |collection| %>
                    <a href="<%=collection_path(collection.address)%>" class="text-gray-700 group flex items-center px-4 py-2 text-sm" role="menuitem" tabindex="-1" id="menu-item-2">
                      <div class="flex items-center">
                        <div class="flex-shrink-0 h-5 w-5">
                          <img class="h-5 w-5 rounded-md" src="<%=collection.image_url%>" alt="">
                        </div>
                        <div class="ml-2">
                          <div class="text-sm font-medium text-gray-900">
                            <%=collection.name%>
                          </div>
                        </div>
                      </div>
                      
                      
                    </a>
                    <% end %>
                    
                  </div>
                  
                  
                </div>
                </div>
              </div>

            </div>
          </div>

          <!-- Mobile menu, show/hide based on menu state. -->
          <div class="md:hidden" id="mobile-menu">
            <div class="px-2 pt-2 pb-3 space-y-1 sm:px-3">
              <!-- Current: "bg-indigo-700 text-white", Default: "text-white hover:bg-indigo-500 hover:bg-opacity-75" -->
              <%# <a href="#" class="bg-indigo-700 text-white block px-3 py-2 rounded-md text-base font-medium" aria-current="page">Dashboard</a> %>
            </div>
          </div>
        </nav>

        <%= yield %>
      </div>
      
    <!-- This example requires Tailwind CSS v2.0+ -->
    <footer class="bg-white">
      <div class="max-w-7xl mx-auto py-12 px-4 text-center sm:px-6 lg:px-8">
        <div class="mt-8 md:mt-0 md:order-1 text-center">
          <p class="text-center text-base text-gray-400 text-lg">
            &copy; <%= Date.today.year %> Sabotage Media, LLC. All rights reserved. <a href="https://github.com/Shpigford/vibewars" class="text-indigo-500 hover:text-indigo-700">Vibe Wars is open source!</a>
          </p>
          <p class="text-center text-base text-gray-400 mt-4">
            <a href="https://forms.gle/HGHrMz5Xt51uRpq18" class="rounded-full py-2 px-4 text-sm bg-indigo-500 text-white hover:bg-indigo-700">Apply to have your project added &rarr;</a>
          </p>
        </div>
      </div>
    </footer>

  </body>
</html>
